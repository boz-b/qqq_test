# __init__.py — This file marks the 'src' folder as a Python "package".
# A package is just a folder that Python treats as a module you can import from.
# Without this file, Python would not recognize 'src' as importable.
# It can be completely empty — its mere existence is enough.
# Example: because this file exists, you can write:
#   from src.features import build_features
# and Python knows to look inside the 'src' folder.
