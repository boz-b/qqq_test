# Copy this file to ../env/finnhub.env before running the news pipeline.
# Keep the real ../env/finnhub.env file local; .gitignore prevents it from being committed.
# Paste the Finnhub API key after the equals sign in the local copy only.
FINNHUB_API_KEY=
